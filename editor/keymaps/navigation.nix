[
  {
    key = "i"; # Remap 'i' to move cursor up (like 'k')
    action = "k";
    options = {
      silent = true;
      noremap = true;
    };
  }
  {
    key = "j"; # Remap 'j' to move cursor left (like 'h')
    action = "h";
    options = {
      silent = true;
      noremap = true;
    };
  }
  {
    key = "k"; # Remap 'k' to move cursor down (like 'j')
    action = "j";
    options = {
      silent = true;
      noremap = true;
    };
  }
  {
    key = "l"; # Keep 'l' to move cursor right (unchanged)
    action = "l";
    options = {
      silent = true;
      noremap = true;
    };
  }
  {
    key = "o"; # Remap 'o' to enter insert mode (like 'i')
    action = "i";
    options = {
      silent = true;
      noremap = true;
    };
  }
]
